{
  "name": "lc-auth",
  "version": "1.1.0",
  "description": "auth library using nodejs builtin features with zero dependencies, allows user to select stateless or stateful strategies and support both credentials and oauth",
  "keywords": [
    "auth"
  ],
  "license": "MIT",
  "author": "cronox20@gmail.com",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "ts-node tests/core.test.ts && ts-node tests/strategies.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
